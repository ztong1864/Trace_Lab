# Benchmark Reproduction

This public release is currently focused on TRACE Lab.  Full paper benchmark
reproduction requires additional datasets/configuration and will be documented
separately.

For collaborators, start with:

```bash
bash scripts/run_lab_api.sh --projects-root runs/lab_projects --host 127.0.0.1 --port 8788
```

